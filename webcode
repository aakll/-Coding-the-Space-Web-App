import React, { useState } from 'react';

export default function App() {
  const [fileName, setFileName] = useState('');
  const [uploadStatus, setUploadStatus] = useState('');

  const handleFileUpload = (e) => {
    const file = e.target.files?.[0];
    if (file && file.name.endsWith('.csv')) {
      setFileName(file.name);
      setUploadStatus('File selected. Click "Apply" to process.');
    } else {
      setFileName('');
      setUploadStatus('Please upload a valid .csv file.');
    }
  };

  const handleSubmit = () => {
    if (!fileName) {
      setUploadStatus('Please select a .csv file before applying.');
      return;
    }
    setUploadStatus('Processing your file...');
    setTimeout(() => {
      setUploadStatus('Prediction complete! View results below.');
    }, 2000);
  };

  const floatingObjects = ['🚀', '🪐', '🌕', '🛰️', '🌠', '🌍', '🌌', '🔭'];

  const teamMembers = [
    {
      name: 'Ali Kawar',
      role: 'Web Developer',
      bio: 'Designing and developing the user-facing web application for model interaction.',
      linkedin: 'https://www.linkedin.com/in/alikawar/ ',
    },
    {
      name: 'Hanin Khalil',
      role: 'Data Scientist',
      bio: 'Analyzing datasets and building predictive models to detect hazardous space objects.',
      linkedin: 'https://www.linkedin.com/in/hanine-khalil-62a7962b8/ ',
    },
    {
      name: 'Samira Jewish',
      role: 'Machine Learning Engineer',
      bio: 'Training and optimizing classification models for accurate hazard detection.',
      linkedin: 'https://www.linkedin.com/in/samira-jawish-366a422b2/ ',
    },
    {
      name: 'Jinan Rachid',
      role: 'UI/UX Designer',
      bio: 'Designing clean and intuitive interfaces for the web application.',
      linkedin: 'https://www.linkedin.com/in/jinan-rachid/ ',
    },
    {
      name: 'Aya Rayed',
      role: 'Frontend Developer',
      bio: 'Implementing responsive and interactive frontend components for the application.',
      linkedin: 'https://www.linkedin.com/in/aya-rayed-01284a2a8/ ',
    },
    {
      name: 'Batoul Hamieh',
      role: 'Project Manager',
      bio: 'Coordinating development and ensuring smooth integration of all components.',
      linkedin: 'https://www.linkedin.com/in/batoul-hamieh/ ',
    },
  ];

  return (
    <div className="min-h-screen bg-black text-white font-sans overflow-x-hidden">
      {/* Sticky Navigation Bar */}
      <nav className="bg-gray-900 py-4 shadow-md fixed top-0 left-0 w-full z-30">
        <div className="container mx-auto flex justify-center gap-8">
          <a href="#header" className="text-white hover:text-blue-400 transition-colors duration-300">Home</a>
          <a href="#about" className="text-white hover:text-blue-400 transition-colors duration-300">About</a>
          <a href="#github" className="text-white hover:text-blue-400 transition-colors duration-300">GitHub</a>
          <a href="#presentation" className="text-white hover:text-blue-400 transition-colors duration-300">Presentation</a>
          <a href="#team" className="text-white hover:text-blue-400 transition-colors duration-300">Team</a>
        </div>
      </nav>

      {/* Header Section */}
      <header id="header" className="relative h-screen flex flex-col items-center justify-center text-center px-4 overflow-hidden">
        {/* Floating Emojis */}
        <div className="absolute inset-0 pointer-events-none z-0">
          {Array.from({ length: 20 }, (_, i) => (
            <div
              key={i}
              className="absolute text-2xl animate-float-bounce"
              style={{
                left: `${Math.random() * 100}%`,
                top: `${Math.random() * 100}%`,
                animationDelay: `${Math.random() * 4}s`,
                animationDuration: `${6 + Math.random() * 6}s`,
              }}
            >
              {floatingObjects[Math.floor(Math.random() * floatingObjects.length)]}
            </div>
          ))}
        </div>

        {/* Background Image */}
        <div className="absolute inset-0 z-0">
          <img
            src="https://live.staticflickr.com/65535/49738502258_3d134eff21.jpg "
            alt="Space background"
            className="w-full h-full object-cover opacity-30"
          />
        </div>

        {/* Main Title */}
        <div className="relative z-10 max-w-3xl">
          <h1 className="text-4xl md:text-6xl font-extrabold mb-4 bg-clip-text text-transparent bg-gradient-to-r from-blue-400 via-purple-500 to-pink-500">
            Space Hazard Predictor
          </h1>
          <p className="text-lg md:text-xl mb-6">
            Upload your dataset to determine if space objects pose a threat to Earth.
          </p>
          <div className="flex flex-col sm:flex-row items-center justify-center gap-4 mt-6">
            <label className="bg-white text-black px-4 py-2 rounded-md shadow-md hover:shadow-lg cursor-pointer transform hover:scale-105 transition-all duration-300">
              Choose File
              <input type="file" accept=".csv" onChange={handleFileUpload} className="hidden" />
            </label>
            <button
              onClick={handleSubmit}
              className="bg-gradient-to-r from-purple-500 to-blue-500 px-6 py-2 rounded-md font-semibold hover:from-purple-600 hover:to-blue-600 transform hover:scale-105 transition-all duration-300"
            >
              Apply
            </button>
          </div>
          <p className="mt-4 text-xl md:text-2xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-blue-400 via-purple-500 to-pink-500">
            Coding the Space
          </p>
          {uploadStatus && <p className="mt-4 text-sm md:text-base text-yellow-300">{uploadStatus}</p>}
        </div>

        {/* Scroll Down Indicator */}
        <div className="absolute bottom-10 left-1/2 transform -translate-x-1/2 z-10">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            className="w-8 h-8 animate-bounce text-blue-400"
          >
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 14l-7 7m0 0l-7-7m7 7V3" />
          </svg>
        </div>
      </header>

      {/* About Section */}
      <section id="about" className="py-16 px-6 bg-gray-800 scroll-mt-16">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-3xl font-bold mb-6">About the Project</h2>
          <p className="text-lg leading-relaxed text-gray-300">
            This web application integrates with a machine learning model that predicts whether an object
            detected in NASA's dataset poses a hazard to Earth. Using a dataset sourced from Kaggle, the model
            analyzes 38 features to classify objects as hazardous or non-hazardous.
          </p>
        </div>
      </section>

      {/* GitHub Section */}
      <section id="github" className="py-16 px-6 bg-black scroll-mt-16">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-3xl font-bold mb-6">GitHub Repository</h2>
          <p className="text-lg leading-relaxed text-gray-300 mb-6">
            Explore the source code for both the machine learning model and the web application on our GitHub repository.
          </p>
          <a
            href="https://github.com/your-repo "
            target="_blank"
            rel="noopener noreferrer"
            className="inline-block bg-gradient-to-r from-green-500 to-teal-500 px-6 py-3 rounded-md font-semibold hover:from-green-600 hover:to-teal-600 transform hover:scale-105 transition-all duration-300"
          >
            View on GitHub
          </a>
        </div>
      </section>

      {/* Presentation Section */}
      <section id="presentation" className="py-16 px-6 bg-gray-800 scroll-mt-16">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-3xl font-bold mb-6">Project Presentation</h2>
          <p className="text-lg leading-relaxed text-gray-300 mb-6">
            Here you can find the full presentation of our project, including methodology, model performance,
            and future improvements.
          </p>
          <a
            href="/path-to-presentation.pdf"
            download
            className="inline-block bg-gradient-to-r from-purple-500 to-indigo-500 px-6 py-3 rounded-md font-semibold hover:from-purple-600 hover:to-indigo-600 transform hover:scale-105 transition-all duration-300"
          >
            Download Presentation
          </a>
        </div>
      </section>

      {/* Team Section */}
      <section id="team" className="py-16 px-6 bg-black scroll-mt-16">
        <div className="max-w-6xl mx-auto">
          <h2 className="text-3xl font-bold text-center mb-12">Meet the Team</h2>
          <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
            {teamMembers.map((member, index) => (
              <div
                key={index}
                className="bg-gray-900 p-6 rounded-lg shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 text-center"
              >
                <div className="w-20 h-20 mx-auto mb-4 rounded-full bg-gradient-to-r from-purple-500 to-blue-500 flex items-center justify-center">
                  <span className="text-2xl font-semibold text-white">{member.name.charAt(0)}</span>
                </div>
                <h3 className="text-xl font-semibold mb-2">{member.name}</h3>
                <p className="text-purple-400 mb-3">{member.role}</p>
                <p className="text-gray-400 text-sm mb-4">{member.bio}</p>
                <a
                  href={member.linkedin}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="block text-blue-400 hover:text-blue-300 font-medium transition-colors duration-300"
                >
                  LinkedIn Profile
                </a>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-gray-900 py-6 text-center text-gray-500">
        <p>© {new Date().getFullYear()} Space Hazard Predictor. All rights reserved.</p>
      </footer>

      {/* Custom Animations */}
      <style jsx>{`
        @keyframes floatBounce {
          0% { transform: translateY(0px) rotate(0deg); opacity: 0.9; }
          50% { transform: translateY(-30px) rotate(180deg); opacity: 1; }
          100% { transform: translateY(0px) rotate(360deg); opacity: 0.9; }
        }
        .animate-float-bounce {
          animation: floatBounce 10s ease-in-out infinite;
        }
      `}</style>
    </div>
  );
}
